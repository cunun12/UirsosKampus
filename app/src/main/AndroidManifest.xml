<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uirsos.www.uirsoskampus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp.LoginActivity" />
        <activity android:name=".Profile.SetupActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" /> <!-- optional (needed if default theme has no action bar) -->

        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".StatusInfo.MainActivity" />
        <activity android:name=".SignUp.RegisterActivity" />
        <activity android:name=".StatusInfo.NewPostActivity" />
        <activity android:name=".StatusInfo.KomentarActivity" />
        <activity android:name=".Profile.ViewPostActivity" />
        <activity android:name=".Profile.FriendActivity" />
        <activity android:name=".Profile.StatusActivity" />
        <activity android:name=".Verifikasi.VerifikasiAccount" />
        <activity android:name=".StatusInfo.DetailBerita" />
        <activity android:name=".Verifikasi.Verifikasi"></activity>
        <activity android:name=".Verifikasi.DetailVerifikasi"/>
    </application>

</manifest>